language: go

go:
- 1.9.x
- 1.10.x
- 1.11.x

before_install:
  - go get -t -v ./...
script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic
  - go build
  - gui/build-linux.sh

after_success:
  - bash <(curl -s https://codecov.io/bash)

notifications:
  email:
    recipients:
      - neochau@gmail.com
    on_success: never